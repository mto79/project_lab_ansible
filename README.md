# [Project_Ansible_Template](#project_ansible_template)

This repo is an Ansible template configured with **Ansible Navigator**.

## Usage

Run the included playbook:

```bash
ansible-navigator run playbooks/example.yml -i inventory/hosts.ini
